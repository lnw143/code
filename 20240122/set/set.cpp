#include<cstdio>
using namespace std;
const int N = 1e3;
int n,k,a[N + 2],f[N + 2][N + 2];
int main() {
    scanf("%d%d",&n,&k);
    for(i=1; i<=n; ++i)
        scanf("%d",&a[i]);
    for(i=1; i<=n; ++i) 
        
    return 0;
}