#include <cstdio>
using namespace std;
const int N = 3e5;
int n,q,tp,a[N + 2],b[N + 2];
int main() {
	freopen("c.in","r",stdin);
	freopen("c.out","w",stdout);
	scanf("%d%d%d",&n,&q,&tp);
	for(int i=1; i<=n; ++i) scanf("%d",&a[i]);
	for(int i=1; i<=n; ++i) scanf("%d",&b[i]);
	
	return 0;
}