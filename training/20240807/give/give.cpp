#include <map>
using ll = long long;
using namespace std;
const int N = 2e2;
int T,n;
ll m,s;
mp<ll,int> f[N + 2];
int main() {
	scanf("%d",&T);
	while(T--) {
		scanf("%lld%lld%d",&m,&s,&n);
		for(int i=1; i<=n; ++i)
			scanf("%d\n",&a[i]);
		
	}
	return 0;
}