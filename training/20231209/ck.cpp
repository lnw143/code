#include<cstdio>
#define run(x) system(#x"_rd.exe > "#x".in");system(#x"_bf.exe < "#x".in > "#x".ans");system(#x".exe < "#x".in > "#x".out");if(system("fc "#x".ans "#x".out")) break;
#include<cstdlib>
using namespace std;
int main() {
	while(true) {
		run(dance);
	}
	return 0;
}