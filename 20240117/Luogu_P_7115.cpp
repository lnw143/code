#include<cstdio>
using namespace std;
const int N = 50, M = 400;
int n,m,a[N + 2][M + 2];
#define color(x) ((x)<=c)
void split(int x,int y,int c) {
	int sum=0;
	for(int i=1; i<=m; ++i)
		sum+=color(a[x][i]);
	
}
int main() {
		
	return 0;
}