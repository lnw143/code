#include<cstdio>
using namespace std;
const int N = 5e3 + 2, P = 998244353;
int n,a[N],s[N],f[N][N],g[N][N],i;
int main() {
	scanf("%d",&n);
	for(i=1; i<=n; ++i) {
		scanf("%d",&a[i]);
		if(~a[i])
			++s[a[i]];
	}
	for(i=1; i<=n; ++i)
		
	return 0;
}